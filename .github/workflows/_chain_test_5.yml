name: Chain test 5

on:
  workflow_dispatch: {}

permissions:
  actions: write

jobs:
  step:
    runs-on: ubuntu-latest
    steps:
      - run: echo "5 실행됨"
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run _chain_test_6.yml --repo "${{ github.repository }}"
