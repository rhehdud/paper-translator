name: Chain test 1

on:
  workflow_dispatch: {}

permissions:
  actions: write

jobs:
  step:
    runs-on: ubuntu-latest
    steps:
      - run: echo "1 실행됨"
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run _chain_test_2.yml --repo "${{ github.repository }}"
