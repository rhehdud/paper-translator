name: Chain test 3

on:
  workflow_dispatch: {}

permissions:
  actions: write

jobs:
  step:
    runs-on: ubuntu-latest
    steps:
      - run: echo "3 실행됨"
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run _chain_test_4.yml --repo "${{ github.repository }}"
